from sqlmodel import SQLModel

class Base(SQLModel):
    """
    Base class for all database models in the application.

    This extends SQLModel to provide a common base for all models.
    """
    pass